// Dependencies:
var orm = require("/connection.js");

// Create methods that will execute the necessary MySQL commands.
// We need methods to retrieve data from the user and store in in the burgers_db.

/*
var orm = {
    // Create an object with methods as follows:

    selectAll()
    insertOne()
    updateOne()
}
*/

// Then we export the orm object:
module.exports = ORM;